{{!< default}}

<div class="gh-page">
    <div class="gh-container">
        <div class="gh-postfeed">
        {{#get "posts" include="authors,tags"}}
        {{#foreach posts}}
            {{> "card"}} {{!-- partials/card.hbs --}}
        {{/foreach}}
        {{/get}}
        </div>
    </div>
</div>
